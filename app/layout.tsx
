import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'The New Updates - Global Editorial News',
  description: 'Breaking news, world affairs, technology, culture, and opinion.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className="bg-brand-bg text-brand-charcoal min-h-screen antialiased">
        {children}
      </body>
    </html>
  );
}
